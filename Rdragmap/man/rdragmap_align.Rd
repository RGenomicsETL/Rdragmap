% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align.R
\name{rdragmap_align}
\alias{rdragmap_align}
\title{Align FASTQ reads with a DRAGMAP v8 reference index}
\usage{
rdragmap_align(
  index,
  read1,
  output_sam,
  read2 = character(),
  executables = rdragmap_executables(),
  read_group_id = "1",
  sample_name = "none",
  threads = 1L,
  fastq_offset = 33L,
  enable_sampling = TRUE,
  preserve_order = TRUE,
  mmap_reference = FALSE
)
}
\arguments{
\item{index}{A validated \code{RdragmapIndex}.}

\item{read1}{Absolute first FASTQ path, optionally gzip-compressed.}

\item{output_sam}{Absolute, not-yet-existing \code{.sam} output path.}

\item{read2}{Empty or one absolute mate FASTQ path.}

\item{executables}{Explicit executable resolution from
\code{rdragmap_executables()}.}

\item{read_group_id}{Read-group identifier recorded in the SAM header.}

\item{sample_name}{Read-group sample recorded in the SAM header.}

\item{threads}{Positive mapper/aligner worker count.}

\item{fastq_offset}{FASTQ quality offset, \code{33L} or \code{64L}.}

\item{enable_sampling}{Whether the native program estimates paired-end
insert-size parameters from the input.}

\item{preserve_order}{Whether to preserve deterministic map/align order.}

\item{mmap_reference}{Whether to memory-map the uncompressed hash, extension,
and reference files. The index must have been built with
\code{write_uncompressed = TRUE} or otherwise contain \code{hash_table.bin} and
\code{extend_table.bin}.}
}
\value{
\code{RdragmapAlignmentResult} or an \code{RdragmapErrorValue}.
}
\description{
Runs the package-owned \code{dragen-os} executable once and writes SAM plus native
mapping metrics beside \code{output_sam}. Paired input also writes native
insert-size statistics. The wrapper preserves map/align order by default so
that the native SAM ordering remains deterministic.
}
\examples{
\donttest{
work <- tempfile("rdragmap-example-")
dir.create(work)
built <- rdragmap_build_index(
  reference_fasta = system.file("extdata", "tiny.fasta", package = "Rdragmap"),
  index_directory = file.path(work, "index"),
  threads = 1L,
  hash_size = "16MB"
)
if (!rdragmap_is_error(built)) {
  aligned <- rdragmap_align(
    index = built@index,
    read1 = system.file("extdata", "one.fastq", package = "Rdragmap"),
    output_sam = file.path(work, "example.sam"),
    read_group_id = "tiny",
    sample_name = "tiny",
    threads = 1L,
    enable_sampling = FALSE
  )
  if (!rdragmap_is_error(aligned)) aligned@outputs
}
unlink(work, recursive = TRUE, force = TRUE)
}
}
