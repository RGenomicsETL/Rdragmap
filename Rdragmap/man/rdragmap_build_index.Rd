% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{rdragmap_build_index}
\alias{rdragmap_build_index}
\title{Build a DRAGMAP v8 reference index}
\usage{
rdragmap_build_index(
  reference_fasta,
  index_directory,
  executables = rdragmap_executables(),
  threads = 1L,
  hash_size = "0GB",
  memory_limit = "0GB",
  seed_length = 21L,
  seed_interval = 1,
  mask_bed = character(),
  decoys = character(),
  max_multi_base_seeds = integer(),
  write_uncompressed = FALSE
)
}
\arguments{
\item{reference_fasta}{Absolute FASTA path.}

\item{index_directory}{Absolute, not-yet-existing output directory.}

\item{executables}{Explicit executable resolution from
\code{rdragmap_executables()}.}

\item{threads}{Positive hash-generation worker count.}

\item{hash_size}{Hash table size with \code{B}, \code{KB}, \code{MB}, or \code{GB} units. \code{"0GB"}
requests the native automatic size calculation.}

\item{memory_limit}{Hash generation memory limit in the same units. \code{"0GB"}
requests the native automatic limit.}

\item{seed_length}{Initial seed length.}

\item{seed_interval}{Reference positions per seed.}

\item{mask_bed}{Empty or one absolute BED path passed to \code{--ht-mask-bed}.}

\item{decoys}{Empty or one absolute FASTA path passed to \code{--ht-decoys}.}

\item{max_multi_base_seeds}{Empty or one non-negative maximum passed to
\code{--ht-max-multi-base-seeds}.}

\item{write_uncompressed}{Whether to retain \code{hash_table.bin} and
\code{extend_table.bin}. These files require substantially more disk space but
permit \code{rdragmap_align(mmap_reference = TRUE)} without decompressing the
hash table for every alignment process.}
}
\value{
\code{RdragmapIndexBuildResult} or an \code{RdragmapErrorValue}.
}
\description{
Runs the package-owned \code{dragen-os} hash-table generator once. The output
directory must not already exist; its parent must exist. The returned index
always contains the compressed hash table. It can also retain the much larger
uncompressed hash and extension tables required for memory-mapped alignment.
}
\examples{
\donttest{
work <- tempfile("rdragmap-example-")
dir.create(work)
built <- rdragmap_build_index(
  reference_fasta = system.file("extdata", "tiny.fasta", package = "Rdragmap"),
  index_directory = file.path(work, "index"),
  threads = 1L,
  hash_size = "16MB"
)
if (!rdragmap_is_error(built)) built@index
unlink(work, recursive = TRUE, force = TRUE)
}
}
