# Explicit dragen-os child executable

Explicit dragen-os child executable

## Usage

``` r
RdragmapExecutables(dragen_os = character(0))
```

## Arguments

- dragen_os:

  Absolute path to `dragen-os`.
